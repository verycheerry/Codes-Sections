// ==========================================
// Author: Vesa
// Student ID: 2024584
// Course: CSCI 221
// Project: Matrix Determinant Calculator
// ==========================================

#include <iostream>
#include <iomanip>
#include <new>
#include <type_traits>
#include <cstdlib>
#include <cstddef> // Vesa - for size_t

using std::cout;
using std::endl;
using std::setw;
using std::ostream;
using std::nothrow;

// Namespace (REQUIRED)
namespace MatrixDetVesa
{
    template <class T>
    class Matrix
    {
    protected:
        // Vesa ID2024584 - storing matrix as 1D array
        T* arr;
        size_t row;
        size_t col;

    public:
        // Constructor
        Matrix(const T* inputArr, size_t r, size_t c);

        // Vesa - virtual for inheritance safety
        virtual ~Matrix();

        // Print functions
        void print() const;
        void print2dAs1DArr() const;

        // Operator overload (declaration only)
        friend ostream& operator<<(ostream& out, const Matrix<T>& m);
    };
}
